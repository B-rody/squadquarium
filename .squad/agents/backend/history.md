# Project Context

- **Project:** squadquarium
- **Created:** 2026-05-05

## Core Context

Backend initialized for squadquarium v0 — owns the Node CLI, the loopback WebSocket bridge, `node-pty` hosting of Squad subprocesses, and the event reconciliation envelope. Distribution target: `npm install -g squadquarium`, run with `squadquarium` or `sqq`.

## Recent Updates

📌 Team initialized 2026-05-05.
📌 No code yet. First task expected: Pre-v0 `node-pty` cross-platform load spike (macOS/Linux/Windows ConPTY).

## Learnings

Initial setup complete.
